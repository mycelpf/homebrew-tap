# This file was generated by GoReleaser. DO NOT EDIT.
cask "mycel" do
  version "0.3.41"

  on_macos do
    on_arm do
      sha256 "1ef9dfaa09e72c0a75d29c465bdd388e86bbd98526973021ad03fa54aad3a2b6"
      url "https://github.com/mycelpf/mycel_cli/releases/download/v#{version}/mycel_#{version}_darwin_arm64.tar.gz"
    end
    on_intel do
      sha256 "46c21a0516ed628b2c38e83bd97f11c84ecfbba1bb3321bc4fbca78cb8258efc"
      url "https://github.com/mycelpf/mycel_cli/releases/download/v#{version}/mycel_#{version}_darwin_amd64.tar.gz"
    end
  end
  on_linux do
    on_arm do
      sha256 "07fe044145ff8a7934cd0e634d641c5835543ffa03948c02d44c7d2bd022dc50"
      url "https://github.com/mycelpf/mycel_cli/releases/download/v#{version}/mycel_#{version}_linux_arm64.tar.gz"
    end
    on_intel do
      sha256 "068051ce2bc8affa3507b58d27413581c92ef38ba37e728880b13b67f2a95a8f"
      url "https://github.com/mycelpf/mycel_cli/releases/download/v#{version}/mycel_#{version}_linux_amd64.tar.gz"
    end
  end

  name "mycel"
  desc "mycel — the umbrella CLI for the mycel_* family (app-builder)"
  homepage "https://github.com/mycelpf/mycel_cli"

  livecheck do
    skip "Auto-generated on release."
  end

  binary "mycel"

  postflight do
    system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/mycel"]
    # Restart every REGISTERED carto daemon. This must iterate the registry, not live
    # processes: `brew upgrade --cask` uninstalls before it installs, so the
    # uninstall.launchctl above has ALREADY unloaded every agent by the time this
    # runs. A "restart what is running" implementation would find nothing, no-op, and
    # leave a clean upgrade with a dead daemon. `carto start --all` reads the registry,
    # which is why it is the right command here.
    #
    # Failure is swallowed on purpose: a daemon that did not come back is not worth
    # failing `brew install` over, and a non-zero postflight aborts or warns the whole
    # operation. `carto start --all` still reports honestly when run by hand.
    system_command "/bin/sh", args: ["-c", "'#{staged_path}/mycel' carto start --all >/dev/null 2>&1 || true"]
  end

  uninstall launchctl: [
      "com.cogniworks.mycel.carto.*",
    ]

  zap trash: [
      "~/.config/mycel/carto",
      "~/Library/Logs/mycel-carto-*.log",
    ]

  caveats <<~EOS
    mycel is ad-hoc signed, not Apple-notarized. This cask clears the download
    quarantine automatically. If macOS still blocks it on first run
    ("mycel cannot be opened because Apple cannot check it for malicious software"),
    allow it once with:
      xattr -dr com.apple.quarantine "$(brew --prefix)/bin/mycel"
    or approve it under System Settings -> Privacy & Security -> Open Anyway.
  EOS
end
